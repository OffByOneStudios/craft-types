#include "../common.h"
#include "../core.h"
#include "cpp_types.h"

using namespace craft;
using namespace craft::types;

CRAFT_TYPE_DEFINE(Type_Node_CppFunction) { _.defaults(); }
CRAFT_TYPE_DEFINE(Type_Property_CppIdentifier) { _.defaults(); }
CRAFT_TYPE_DEFINE(Type_Property_CppStaticDescription) { _.defaults(); }
CRAFT_TYPE_DEFINE(Type_Edge_CppLegacyImplements) { _.defaults(); }