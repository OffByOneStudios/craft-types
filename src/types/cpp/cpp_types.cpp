#include "cpp_types.h"

using namespace craft;

CRAFT_TYPE_DEFINE(types::Type_Node_CppFunction) { _.defaults(); }
CRAFT_TYPE_DEFINE(types::Type_Property_CppIdentifier) { _.defaults(); }
CRAFT_TYPE_DEFINE(types::Type_Property_CppStaticDescription) { _.defaults(); }
CRAFT_TYPE_DEFINE(types::Type_Edge_CppLegacyImplements) { _.defaults(); }